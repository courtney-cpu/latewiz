// Stub — SDK calls are now proxied through server-side API routes.
// Kept for backwards compatibility with any remaining imports.
export function useLate() { return null; }
export function useLateClient() { return null; }
